CREATE TABLE STUDENT (UID BIGINT IDENTITY NOT NULL, ADDRESS VARCHAR, COLLEGE_CODE VARCHAR, NAME VARCHAR, ROLL_NO VARCHAR, fk_course_code VARCHAR, PRIMARY KEY (UID))
CREATE TABLE COURSE (CODE VARCHAR NOT NULL, DURATION INTEGER, NAME VARCHAR, SHORT_CODE VARCHAR, PRIMARY KEY (CODE))
ALTER TABLE STUDENT ADD CONSTRAINT FK_STUDENT_fk_course_code FOREIGN KEY (fk_course_code) REFERENCES COURSE (CODE)
